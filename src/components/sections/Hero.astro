---
import BrowserFrame from '../ui/BrowserFrame.astro';

const macReleaseUrl = 'https://github.com/navam-io/stagent/releases/latest';
const macReleaseApiUrl = 'https://api.github.com/repos/navam-io/stagent/releases/latest';
---
<section class="relative min-h-screen flex flex-col items-center justify-center px-6 overflow-hidden pt-24 pb-16">
  <!-- Perspective grid -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden" aria-hidden="true">
    <div class="hero-grid"></div>
  </div>

  <!-- Dual atmospheric glow -->
  <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
    <div class="absolute w-[800px] h-[800px] rounded-full bg-primary/[0.10] blur-[180px]"></div>
    <div class="absolute w-[400px] h-[400px] rounded-full bg-primary/[0.15] blur-[100px]"></div>
  </div>

  <!-- Bottom gradient fade into next section (bg-surface-raised) -->
  <div class="absolute bottom-0 left-0 right-0 h-32 pointer-events-none" style="background: linear-gradient(to bottom, transparent, var(--color-surface-raised));"></div>

  <!-- Content -->
  <div class="relative z-10 text-center max-w-4xl">
    <h1
      data-animate
      class="font-mono text-[clamp(2.2rem,10vw,7rem)] tracking-[0.2em] sm:tracking-[0.5em] font-normal mb-6 text-text whitespace-nowrap text-center"
      style="padding-left: 0.2em;"
    >
      STAGENT
    </h1>

    <p
      data-animate
      class="text-[clamp(0.95rem,2.5vw,1.5rem)] text-text-muted leading-relaxed mb-8 max-w-2xl mx-auto"
      style="transition-delay: 120ms"
    >
      Governed AI agent operations with reusable profiles, workflow blueprints, scheduled runs, and local-first oversight
    </p>

    <!-- Stats bar -->
    <div
      data-animate
      class="flex items-center justify-center gap-3 sm:gap-8 mb-10 flex-wrap font-mono text-xs tracking-widest text-text-muted"
      style="transition-delay: 200ms"
    >
      <span>Profiles &amp; Policies</span>
      <span class="text-border">·</span>
      <span>Blueprints &amp; Schedules</span>
      <span class="text-border">·</span>
      <span>Open Source</span>
    </div>

    <p
      data-animate
      class="font-mono text-xs tracking-[0.28em] uppercase text-primary mb-5"
      style="transition-delay: 260ms"
    >
      Download the macOS desktop app today
    </p>

    <p
      data-animate
      class="text-sm sm:text-base text-text-muted leading-relaxed mb-10 max-w-xl mx-auto"
      style="transition-delay: 300ms"
    >
      Install the latest macOS build and start governing agent runs, approvals, and reusable workflows directly from your Mac.
    </p>

    <!-- CTAs -->
    <div data-animate class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-16" style="transition-delay: 340ms">
      <div class="w-full sm:w-auto">
        <a
          id="desktop-download-link"
          href={macReleaseUrl}
          class="inline-flex w-full min-w-[20rem] items-center justify-center gap-3 whitespace-nowrap rounded-md bg-primary px-8 py-4 font-mono text-xs font-semibold uppercase tracking-[0.2em] text-surface transition-colors hover:bg-primary/90 primary-pulse sm:w-auto"
        >
          <span>Download Desktop App for Mac</span>
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" aria-hidden="true">
            <path d="M12 4v12" />
            <path d="M7 11l5 5 5-5" />
            <path d="M5 20h14" />
          </svg>
        </a>
        <p id="desktop-download-meta" class="mt-3 text-center font-mono text-[0.65rem] uppercase tracking-[0.24em] text-text-dim">
          Latest release • DMG on GitHub
        </p>
      </div>
      <a
        href="/docs"
        class="font-mono text-xs tracking-[0.2em] uppercase text-text-muted border border-border px-8 py-3 hover:border-primary hover:text-primary transition-colors rounded-md"
      >
        View Docs
      </a>
    </div>

    <!-- Hero screenshot -->
    <div data-animate style="transition-delay: 400ms" class="max-w-4xl mx-auto">
      <BrowserFrame
        src="/screenshots/home-list.png"
        alt="Stagent home workspace showing active tasks, pending reviews, project metrics, and live agent activity"
        perspective={true}
      />
    </div>
  </div>

  <!-- Scroll chevron -->
  <div class="absolute bottom-10 left-1/2 -translate-x-1/2 animate-bounce opacity-50">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
      <path d="M6 9l6 6 6-6" />
    </svg>
  </div>
</section>

<script is:inline define:vars={{ macReleaseApiUrl }}>
  const link = document.getElementById('desktop-download-link');
  const meta = document.getElementById('desktop-download-meta');

  if (link instanceof HTMLAnchorElement) {
    fetch(macReleaseApiUrl)
      .then(async (response) => {
        if (!response.ok) {
          throw new Error(`GitHub release lookup failed with status ${response.status}`);
        }

        return response.json();
      })
      .then((release) => {
        const macAsset = release.assets?.find((asset) => typeof asset?.name === 'string' && /\.dmg$/i.test(asset.name));

        if (!macAsset?.browser_download_url) {
          return;
        }

        link.href = macAsset.browser_download_url;

        if (meta) {
          meta.textContent = `${release.tag_name ?? 'Latest release'} • ${macAsset.name}`;
        }
      })
      .catch(() => {
        if (meta) {
          meta.textContent = 'Latest release • DMG on GitHub';
        }
      });
  }
</script>

<style>
  .hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      repeating-linear-gradient(
        90deg,
        var(--color-border) 0 1px,
        transparent 1px 60px
      ),
      repeating-linear-gradient(
        0deg,
        var(--color-border) 0 1px,
        transparent 1px 60px
      );
    opacity: 0.2;
    transform: perspective(600px) rotateX(55deg);
    transform-origin: center top;
    mask-image: radial-gradient(ellipse 80% 60% at 50% 30%, black 20%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse 80% 60% at 50% 30%, black 20%, transparent 70%);
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-grid {
      transform: none;
    }
  }
</style>
